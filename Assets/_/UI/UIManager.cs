using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIManager : MonoBehaviour
{
    public PrefabList UIPrefabList;

    // Deveria ter um Script que carrege os métodos
    // Deveria ter um sistema de transição
    // Deveria ter um prefab para cada menu
    // Deveria ter um script para cada menu
    // Loading screen deveria receber parâmetros (fundo, dica, 3D scene ou shader, barra carregando, etc) 

    // UI Prefab deveria colocar determinado painel à frente
    // Talvez algo para controlar os padrões, como um StateMachine

    // UI deveria se inscrever em algo que envie o "Pause", ou que envie o "Loading", ou ainda que envie o "Menu"
}
