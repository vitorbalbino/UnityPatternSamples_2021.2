using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManager : MonoBehaviour
{
    List<Player> Players;
}



public class Player {
    // Player ID

    // PlayerType (local, online, I.A.)

    // Player data (Ranking, conquests)

    // Game Data (prefs, Controlled Characters and game data)

    // Actions (Controls and input actions)

    PlayerToWorldState ToWorldState;

    // Online Representation (Write and read when online)
}
